# Core ML
torch>=2.4.0
transformers==4.52.4
accelerate>=1.0.0
peft>=0.13.0
datasets>=3.0.0
trl>=0.12.0

# Quantization (use bitsandbytes only on NVIDIA; on LUMI AMD use native bf16)
# bitsandbytes>=0.43.0  # NVIDIA only — comment out on LUMI ROCm

# Tokenizer
tokenizers>=0.20.0
sentencepiece

# Notebook
jupyterlab>=4.2.0
ipywidgets>=8.1.0

# Utilities
huggingface_hub>=0.25.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
tqdm>=4.66.0
psutil>=6.0.0

# Logging / experiment tracking
wandb>=0.18.0
tensorboard>=2.18.0

# Flash Attention (optional, improves throughput)
# flash-attn>=2.6.0  # requires CUDA; for ROCm use: pip install flash-attn --no-build-isolation
